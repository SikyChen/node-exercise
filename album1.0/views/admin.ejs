<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>上传图片</title>
  <link rel="stylesheet" href="/css/bootstrap.css">
  <style>
    .up-form{
      width: 90%;
      margin: 0 auto;
    }
    .alert{
      width: 90%;
      margin-left: auto;
      margin-right: auto;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-inverse">
    <div class="container-fluid">

      <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">空间相册</a>
      </div>

      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><a href="/">相册列表</a></li>
          <li class="active"><a href="#">上传图片<span class="sr-only">(current)</span></a></li>
        </ul>
      </div><!-- /.navbar-collapse -->

    </div><!-- /.container-fluid -->
  </nav>
  
  <% if(status == 1) { %>
  <div class="alert alert-success" role="alert">图片上传成功！</div>
  <% } %>
  <% if(status == 2) { %>
  <div class="alert alert-danger" role="alert">图片上传失败！</div>
  <% } %>

  <form class="up-form" action="/upimage" method="post" enctype="multipart/form-data">
    <div class="form-group">
      <% if(allAlbums.length == 0) { %>
      没有相册，请创建相册
      <% } %>
      <% if(allAlbums.length > 0) { %>
      <label>选择上传相册</label>
      <select class="form-control" name="albumName">
        <% for(let i=0; i<allAlbums.length; i++) { %>
        <option><%= allAlbums[i] %></option>
        <% } %>
      </select>
      <% } %>
      <p class="help-block">
        <a href="/createAlbum" id="createAlbum">创建相册</a>
      </p>
    </div>
    <div class="form-group">
      <label for="exampleInputFile">选择要上传的图片</label>
      <input type="file" id="exampleInputFile" name="upImage">
      <p class="help-block">图片不应大于2M</p>
    </div>
    <button type="submit" class="btn btn-default">Submit</button>
  </form>

  <script src="/js/jquery-1.11.3.min.js"></script>
  <script src="/js/bootstrap.min.js"></script>
</body>
</html>